# Itqan Upgrade — Security & Code Audit

**Date:** 2026-06-26
**Stack:** Next.js 16, React 19, TypeScript 5.7, PostgreSQL (pg) + Supabase, Tailwind CSS 4, Vitest

---

## PASS 1 — Full Findings

### 1. Security

#### CRITICAL (All Fixed)

| # | Issue | File + Line | Status |
|---|-------|-------------|--------|
| S1 | Middleware file dead code (`proxy.ts` never executed by Next.js) | `proxy.ts` (root) | **Fixed** — renamed to `middleware.ts` |
| S2 | Hardcoded Supabase service role key | `scripts/test-admin-apis.js:11`, `scripts/update_smtp_direct.ts:4` | Not fixed — scripts need manual cleanup |
| S3 | Hardcoded DB connection strings with passwords | `scripts/migrate-005.js:4`, `migrate-006.js:4`, `migrate-006b.js:4`, `fix-halaqat-gender.js:3`, `fix-constraints.js:5`, `check_constraint.js:2` | Not fixed — scripts need manual cleanup |
| S4 | Hardcoded Gmail app password | `scripts/update_smtp_direct.ts:13`, `scripts/update_smtp.ts:10` | Not fixed — scripts need manual cleanup |
| S5 | Hardcoded JWT secret fallback | `lib/auth.ts:5` | **Fixed** — throws in production if `JWT_SECRET` unset |
| S6 | XSS via `javascript:` URIs in markdown | `lib/community/markdown.ts:27-36` | **Fixed** — added `isSafeUrl()` protocol allowlist |
| S7 | 7 unauthenticated cron endpoints | `app/api/cron/check-expirations/`, `auto-end-sessions/`, `recover-multipart/`, `reminders/`, `session-reminders/`, `streak-reminders/`, `task-reminders/` | **Fixed** — added `isCronAuthorized()` to all |
| S8 | Unauthenticated SSRF proxy (audio-proxy) | `app/api/audio-proxy/route.ts:19` | **Fixed** — added URL allowlist matching pdf-proxy |

#### HIGH

| # | Issue | File + Line | Status |
|---|-------|-------------|--------|
| S9 | Cron auth fail-open when `CRON_SECRET` unset | `cron/academy-reminders:46`, `cron/parent-weekly-reports:6`, `cron/admin-activity-reports:7` | **Fixed** — fail closed in production |
| S10 | `CRON_SECRET` accepted via URL query params (gets logged) | Same 3 files | **Fixed** — removed query param acceptance |
| S11 | Meeting passwords in public API | `app/api/public/lessons/[slug]/route.ts:62`, `view/route.ts:61` | **Fixed** — removed password from response and SQL query |
| S12 | `SELECT *` returns `password_hash` | `lib/db-queries/user.ts` (9 functions) | **Fixed** — explicit safe column list, also fixed `auth/verify` |

#### MEDIUM

| # | Issue | File + Line | Status |
|---|-------|-------------|--------|
| S13 | JSON-LD script injection (`</script>` not escaped) | `components/json-ld-schema.tsx:97` | Not fixed |
| S14 | Wildcard image remote pattern (`hostname: '**'`) | `next.config.mjs:13` | Not fixed |
| S15 | SSL verification disabled | `lib/db.ts:36`, `server.js:35` | Not fixed — Supabase pooler requires it |
| S16 | DB host logged on startup | `lib/db.ts:62` | **Fixed** — removed log line |
| S17 | `.gitignore` incomplete (no `.env`, no `scripts/`) | `.gitignore:9` | Not fixed |

---

### 2. Dependencies

**npm audit found 11 vulnerabilities** (4 high, 6 moderate, 1 low).

#### Safe to auto-fix (patch/minor):

| Package | Severity | Issue |
|---------|----------|-------|
| `@babel/core` ≤7.29.0 | LOW | Arbitrary file read via sourceMappingURL |
| `form-data` 4.0.0-4.0.5 | HIGH | CRLF injection in multipart fields |
| `undici` 7.0.0-7.27.2 | HIGH | 7 vulns: TLS bypass, HTTP injection, etc. |
| `tmp` <0.2.6 | HIGH | Path traversal |
| `js-yaml` ≤4.1.1 | MODERATE | Quadratic DoS |

#### Needs decision (breaking changes):

| Package | Severity | Issue | Breaking Upgrade |
|---------|----------|-------|-----------------|
| `nodemailer` ≤9.0.0 | HIGH | CRLF injection, SSRF | Requires `next-auth@1.12.1` |
| `postcss` <8.5.10 | MODERATE | XSS via `</style>` | Requires `next@9.3.3` |
| `uuid` <11.1.1 | MODERATE | Buffer bounds check | Requires `exceljs@3.4.0` |

---

### 3. Duplicated Logic

| # | What | Files | Impact |
|---|------|-------|--------|
| D1 | Upload route logic (identical sanitization) | `app/api/upload/route.ts`, `upload-pdf/route.ts`, `upload-audio/route.ts` | ~80 lines |
| D2 | Competition routes (library vs academy) | 12 files under `admin/competitions/` and `academy/admin/competitions/` | ~500 lines |
| D3 | Dashboard shell fetch patterns | `components/dashboard-shell.tsx`, `academy-dashboard-shell.tsx` | ~60 lines |
| D4 | Session + auth check boilerplate | 100+ API route files | ~450 lines |
| D5 | Community like toggle | 3 route files under `community/` | ~100 lines |

---

### 4. Obvious Refactors

| # | What | File | Action |
|---|------|------|--------|
| R1 | Dead code: never-imported template file | `lib/api-endpoint-templates.ts` (410 lines) | Delete |
| R2 | Dead code: one-off migration scripts at root | `run-032.ts`, `run-033.ts` | Delete |
| R3 | Dead code: junk files at root | `temp.html`, `student_center_old.tsx` | Delete |
| R4 | No-op i18n pattern (always returns fallback) | 20+ files, 500+ occurrences | Replace with direct string |
| R5 | Duplicate `awardPoints` in two modules | `lib/academy/points.ts` vs `lib/academy/gamification.ts` | Consolidate |
| R6 | Oversized files | `sessions-hub.tsx` (1468 lines), `admin-center.tsx` (1310 lines), `competitions.ts` (1118 lines) | Split when touching |

---

### 5. Reusable Pieces

| # | What | Where | Recommendation |
|---|------|-------|----------------|
| U1 | `uploadFile()` utility | 3 upload routes share identical sanitization | Extract to `lib/storage.ts` |
| U2 | `requireAdmin()` helper | 35+ admin routes repeat the same guard | Use existing `requireRole()` |
| U3 | `toggleLike()` utility | 3 community like routes | Extract to `lib/community/likes.ts` |

---

### 6. Health Checks

| # | Issue | File(s) | Risk |
|---|-------|---------|------|
| H1 | No try/catch on entire API handlers | `student/progress-report`, `student/specializations`, `student/family`, `halaqat/[id]/students` | **Fixed** — all handlers wrapped in try/catch |
| H2 | N+1 queries in cron loops | `cron/streak-reminders`, `task-reminders`, `session-reminders`, `werd-reminders` | Individual INSERT per student |
| H3 | No pagination on list endpoints | `app/api/readers/`, `library/books/` | Returns all rows |
| H4 | New Supabase client per request | `academy/teacher/tasks/[id]/submissions/[submissionId]/grade/route.ts:16` | Wasteful |
| H5 | HTTP not HTTPS | `lib/geo.ts:64` | `fetch('http://ip-api.com/...')` |

---

## PASS 2 — Fixes Applied

### Group 1: Critical Security Fixes ✅

| Fix | File | What Changed |
|-----|------|-------------|
| S1 | `proxy.ts` → `middleware.ts` | Renamed — Next.js now executes the auth middleware |
| S5 | `lib/auth.ts` | JWT secret fallback throws in production |
| S6 | `lib/community/markdown.ts` | Added `isSafeUrl()` — blocks `javascript:` URIs |
| S7 | 7 cron routes | Added `isCronAuthorized()` to all GET handlers |
| S8 | `app/api/audio-proxy/route.ts` | Added URL allowlist (utfs.io, ufs.sh, amazonaws.com, cloudinary.com) |
| S9/S10 | 3 cron routes | Fail closed in production; removed query param auth |
| S16 | `lib/db.ts` | Removed DB host log |

**New file:** `lib/cron-auth.ts` — shared cron authorization utility.

### Group 2: Dependencies (Patch/Minor) ✅

Ran `npm audit fix --legacy-peer-deps`. Reduced vulnerabilities from 11 → 6.

**Fixed (patch/minor):**
- `@babel/core` — arbitrary file read via sourceMappingURL
- `form-data` — CRLF injection in multipart fields
- `undici` — 7 vulnerabilities (TLS bypass, HTTP injection, etc.)
- `tmp` — path traversal
- `js-yaml` — quadratic DoS

**Remaining (require breaking changes — not applied):**
- `nodemailer` ≤9.0.0 (HIGH) — requires next-auth major upgrade
- `postcss` <8.5.10 (MODERATE) — requires next@9.3.3 (downgrade from 16!)
- `uuid` <11.1.1 (MODERATE) — requires exceljs@3.4.0

### Group 3: Safe Cleanups ✅

| Fix | What |
|-----|------|
| R1 | Deleted `lib/api-endpoint-templates.ts` (410 lines dead code) |
| R2 | Deleted `run-032.ts`, `run-033.ts` (one-off migration scripts) |
| R3 | Deleted `temp.html`, `student_center_old.tsx` (junk files) |
| S17 | Updated `.gitignore` — added `.env`, `.env.*`, temp files |

---

## Action Items for Owner

1. **Rotate all exposed credentials** — Supabase service key, DB passwords, Gmail app password
2. **Set `CRON_SECRET` env var** in production
3. **Set `JWT_SECRET` env var** in production (if not already set)
4. **Test middleware activation** — verify login flow and public pages still work
5. **Delete or rewrite scripts** with hardcoded secrets (`scripts/test-admin-apis.js`, `scripts/update_smtp_direct.ts`, etc.)
6. **Purge secrets from Git history** — use `git filter-repo` or BFG Repo Cleaner
7. **Consider pre-commit secret scanner** (gitleaks, detect-secrets)
